# `zx-bench` 与当前 `llm-test` 对比

调研日期：2026-09-07

上游仓库：[suncityldp/zx-bench](https://github.com/suncityldp/zx-bench)

## 结论

两者不是同一类榜单的替代品。

- 当前 `llm-test` 更适合做本地模型、量化版本、KV 量化和推理开关之间的可重复差分测试。
- `zx-bench` 更像面向真实开发工作的场景评测，重点是代码修复、CLI/工具工作流、数据抽取、幻觉抵抗、结构化输出和安全权限。
- `zx-bench` 的题目不能直接无缝接入当前项目。可以复用它的题目定义和部分评分思路，但需要增加题型适配器、运行时字段映射，以及多语言沙箱能力。

## 规模与测试口径

`zx-bench` 当前 `benchmark-meta.json` 声明：574 道有效题，另有 78 道退役题，累计 652 道。有效题的维度为：

| 维度 | 题数 |
|---|---:|
| 编程 | 150 |
| 幻觉抵抗 | 78 |
| 推理与数学 | 34 |
| 指令遵循 | 42 |
| 安全与权限 | 50 |
| 智能体工作流 | 45 |
| 工具/CLI 工作流 | 56 |
| 数据抽取 | 35 |
| 深度 CLI 任务 | 56 |
| 结构化输出 | 28 |

它不是单纯的选择题集，主要口径是：

- 编程题把模型输出放进沙箱，实际编译、执行公开/隐藏测试；覆盖 JavaScript、TypeScript、Python、Go、Java、C/C++、Rust、PHP、C#、Bash、SQL 等。
- 编程题包含 `no_bug` 陷阱题，模型强行修改本来正确的代码会扣分。
- 题目带 `difficulty`、`dimension`、`scenarioVersion`、`scenarioHash`、标签和 grader 版本，评分结果包含证据和输出元数据。
- 确定性规则评分与 AI Judge 并存，综合分按难度和能力维度加权，不等同于公开 benchmark 的 `correct / total`。
- 支持单题硬时限、思考/输出约束、指数退避重试、流式 TTFT/生成速度、题目级断点续跑。仓库说明常按 300 秒设计；当前 caller 在没有运行级硬时限时的 fallback 是 10 分钟，实际以运行配置为准，流式两次 chunk 间还有最长 180 秒的 idle timeout。
- 多轮评测对每道题串行执行，并保存均值、中位数、标准差、Bootstrap 置信区间、一致性和截断率；批量题目使用全局或按维度的并发 worker。

当前 `llm-test` 的口径不同：使用 GPQA、AIME、MMLU-Pro、LongBench、代码/指令/安全数据集，主要是明确答案、官方校验器或真实代码沙箱，最终分数直观地保留 `unknown` 在分母中。它更适合在固定题目、固定采样参数下比较两个本地模型版本，而不是给复杂工程能力做综合排名。

## 优劣与侧重点

### 当前 `llm-test` 的优势

- 本地优先，直接连接 OpenAI 兼容端点/llama-swap，配置成本低。
- 题库规模和官方数据集口径更清晰，适合比较量化层级、KV 量化、思考开关和上下文配置。
- 选择题和可验证任务评分透明，错误、未知、输出截断可以分开观察。
- 已有速度测试、重复轮次、题目并发、运行日志、历史对比和断点恢复，适合做工程调参实验。

### 当前 `llm-test` 的不足

- 真实工作流题目较少，CLI、工具调用、多文件工程修复和长期任务覆盖不足。
- 目前不同题型的综合比较仍以项目分数为主，没有 `zx-bench` 那种难度/维度加权的综合治理层。
- 多语言代码沙箱和场景证据链没有 `zx-bench` 完整。

### `zx-bench` 的优势

- 更贴近“给模型一个真实开发任务，看它能否完成”的场景，而不是只看最终答案。
- 代码题真实执行隐藏测试，能发现看似正确但实际不能编译、边界条件错误或误改正确代码的情况。
- 题目版本、hash、grader、证据和运行元数据较完整，适合长期回归测试。
- 有实践型维度：CLI、工具调用、Agent trace、数据抽取、幻觉抵抗和安全权限。

### `zx-bench` 的不足

- 依赖更重：Node 22、pnpm、Prisma/SQLite、Docker，以及多语言运行镜像。
- AI Judge 和加权综合分会引入额外耗时与解释成本，不适合直接当作量化损失的唯一指标。
- 574 题是作者维护的场景集合，不是 GPQA/AIME 这类已有社区共识的公开榜单口径。
- Docker 镜像首次准备、编译和隐藏测试会让单题耗时波动很大；不同机器的总时间可比性弱于纯选择题。

## 能否直接接入

不能直接把 `benchmark.json` 放进当前 `scripts/data/` 后运行。主要不兼容点：

1. 当前项目按 `task.kind` 分派少数固定 runner；`zx-bench` 按 `scenario.grader` 分派十多个 evaluator。
2. 当前题目输入主要是 JSONL/Parquet 转成统一 prompt；`zx-bench` 题目还包含源文件、fixture、公开/隐藏测试、沙箱需求、版本 hash 和多文件工程结构。
3. 当前判题服务主要围绕现有 Python/脚本题；`zx-bench` 需要多语言容器、编译器、依赖镜像、资源限制和 no-egress 隔离。
4. 当前项目的分数是各题正确/错误/未知的直接聚合；`zx-bench` 还要保存轴分、Judge 分、难度权重、维度权重、证据和人工复核标志。

可行的接入方式有两种：

- **轻量接入，推荐先做**：挑选 20-50 道 `zx-bench` 场景，转成当前项目的 `custom` JSONL；只接入 `exact_answer_line`、`json_atomic_fields`、部分 `instruction_checklist` 和 Python `code_repair`，将结果作为“实战小套件”，不宣称等价于完整 `zx-bench`。
- **完整适配**：新增 scenario manifest、稳定题目 ID/hash、grader registry、每题超时/重试/证据字段、多语言 Docker runner 和加权报告。这样才有资格说支持 `zx-bench`，工作量明显大于增加一个普通数据集。

最值得借鉴的实现点：

- 用稳定 `scenarioId + scenarioHash + graderVersion` 做断点和结果主键，不只依赖数组下标。
- 把模型错误、网络错误、判题环境错误、超时和截断分开保存。
- 保存原始输出、思考输出、finish reason、输入/输出 token、TTFT、生成速度和证据摘要。
- 对代码题执行真正的隐藏测试，并加入 `no_bug` 题，避免模型靠改写或关键词伪装得分。
- 采用单题硬超时与流式 idle timeout，避免一个请求无限占用整个队列。

## 跑一轮大概多久

没有脱离模型和并发配置的固定答案。粗略公式是：

```text
总耗时 ≈ 有效题数 ÷ 有效并发 × 单题模型生成时间
       + 沙箱/编译时间 + Judge 时间 + 重试和超时损失
```

当前 Q8 运行 `mtqn60d7` 用 5 个项目跑了 528 个采样题（198+100+100+100+30）和速度测试，记录耗时约 409 分钟；其中 GPQA 的题目并发为 2，并且发生了多次 5 分钟请求超时。这个实测只能作为同一机器、同一模型的保守参照。

因此对 `zx-bench` 的 574 道有效题，建议先按以下范围安排：

- 思考关闭、并发 2、无大量 Judge 重试：约 2-6 小时。
- 当前 Q8 这类本地模型、部分思考开启、含代码编译/隐藏测试：约 6-12 小时更现实。
- 如果大量请求接近 5-10 分钟超时，最坏情况会超过 24 小时；不建议直接从全量开始。

建议先跑 20-50 道覆盖不同维度和难度的 pilot，确认模型调用、Docker 沙箱、评分和断点都正常，再决定全量。
